/**
 * OpinionController
 *
 * @description :: Server-side actions for handling incoming requests.
 * @help        :: See https://sailsjs.com/docs/concepts/actions
 */



module.exports = {

    //function beta
    /*create: async (req, res) => {
        const userend = await UserEnd.find({ name_alias: req.body.userend })
        console.log(userend),
        await Opinion.create({
            texto: req.body.texto,
            fecha: req.body.fecha,
            userend: userend._id
        })
            .then(opinion => {
                return res.send({
                    'data': opinion,
                })
            })
            .catch(err => {
                return res.send({
                    'error': err,
                })
            })
    }*/

};

