import { PolarityBackgroundDirective } from './polarity-background.directive';

describe('PolarityBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new PolarityBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
